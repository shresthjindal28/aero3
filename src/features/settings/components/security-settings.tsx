export function SecuritySettings() {
  return (
    <section className="rounded-xl border border-border/60 bg-card/50 p-6">
      <h2 className="text-base font-semibold">Security settings</h2>
      <p className="mt-2 text-sm leading-relaxed text-muted-foreground">
        Password changes, active sessions, and login history are not available yet. Your
        account is still protected by secure sign-in.
      </p>
    </section>
  );
}
